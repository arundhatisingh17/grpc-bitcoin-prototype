#include <iostream>
#include "node.h"
#include "grpc_server.h"
#include "grpc_client.h"

int main() {
    std::cout << "Starting Bitcoin Node Prototype..." << std::endl;
    
    // We will build out the simulation logic here next!
    // For now, this just proves that all our files compile and link successfully.
    
    return 0;
}
